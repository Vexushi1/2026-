# 模型论文框架

> 本文件只保留当前有效口径。模型、参数、结果和图表发生变化时，直接替换旧内容；历史由 Git 保存。

- 项目：`2026年第五届“创新杯”大学生大数据挑战赛 A题`
- 版本：`v0.7-current`
- 当前阶段：`问题一、问题二已形成模型—结果—图表闭环；问题三代码已输出，待本机运行返回结果`
- 最近同步：`问题二正式图表解释；问题三双结构先验与统计回退模型`
- 更新时间：`2026-07-25`

## 1 当前总主线

```text
数据审计
→ 结构关系识别
→ 任务适配模型
→ 分层交叉验证
→ 中文结果工作簿
→ MATLAB科研绘图
→ DOCX草稿
→ LaTeX终稿
```

统一规则：

1. Python 只负责处理、求解、检验和结果表。
2. MATLAB 只读取 Python 输出绘图，不重新计算核心结果。
3. 核心结果优先单图；只有支撑同一结论的图才组成小规模多面板。
4. 能用表格清楚表达的结果不机械作图。
5. 竞赛主指标为三问 Accuracy 加权和，辅助报告 Macro-F1、Balanced Accuracy、误差结构和稳定性。

## 2 数据审计当前结论

- 原始数据：10000 行、64 列。
- 完全重复行：0。
- 重复 `Person_ID`：0。
- 真实空值：0。
- 字符串 `None` 为显式类别，不视为缺失。
- 所有硬边界违反数为 0。
- `Fitness_Level=Wellness_Category` 逐样本一致率为 100%。
- `Health_Score` 在 44.95、64.95、79.95 下可完全恢复 `Wellness_Category`。
- `Early_Waker` 与 06:30 起床边界完全一致。
- `Wellness_Category` 分布：Poor 114、Average 2137、Good 4429、Excellent 3320。

## 3 问题一冻结口径

### 3.1 模型

设起床时刻分钟数为 $t_i$，阈值模型为

$$
\tau^*=\arg\min_{\tau}\frac1n\sum_i
\mathbb I\!\left[\mathbb I(t_i<\tau)\ne y_i\right].
$$

数据满足

$$
\max_{y_i=1}t_i=389,\qquad
\min_{y_i=0}t_i=390,
$$

因此理论零误判区间为

$$
389<\tau\le390.
$$

采用代表性阈值

$$
\tau^*=389.5\text{ min}=06{:}29{:}30.
$$

等价业务规则：

$$
Wake\_Up\_Time<06{:}30
\Longrightarrow Early\_Waker=Yes.
$$

### 3.2 结果

| 指标 | 数值 |
|---|---:|
| OOF ACC | 1.0000 |
| Precision | 1.0000 |
| Recall | 1.0000 |
| F1 | 1.0000 |
| Balanced Accuracy | 1.0000 |
| Brier Score | 0.001870 |
| LogLoss | 0.007891 |
| 误判数 | 0 |

### 3.3 图表证据链

| 图号 | 图名 | 类型 | 结论 | 状态 |
|---|---|---|---|---|
| 图5-1 | 早起标签与起床时间分界 | 单图 | 06:30 前后存在严格时间分界 | 已完成 |
| 图8-1 | 阈值敏感性与测量扰动鲁棒性 | 2面板 | 阈值在 $(389,390]$ 内零误判，测量扰动增大时 ACC 下降 | 已完成 |
| 图8-2 | 重抽样与交叉验证稳定性 | 2面板 | Bootstrap 与 40 个验证折均保持阈值不变 | 已完成 |
| 图A-1 | 标签噪声下的阈值恢复能力 | 单图 | 全局随机标签噪声下结构边界仍可恢复 | 已完成 |

## 4 问题二冻结口径

### 4.1 等级定义

统一四分位切点为

$$
c_1=66.0,\qquad c_2=74.6,\qquad c_3=82.8.
$$

定义

$$
Y_i^{(2)}=
\begin{cases}
0,&H_i<66.0,\\
1,&66.0\le H_i<74.6,\\
2,&74.6\le H_i<82.8,\\
3,&H_i\ge82.8.
\end{cases}
$$

对应 `Poor`、`Average`、`Good`、`Excellent`。

### 4.2 模型

采用

$$
\text{固定四分位等级}
+\text{CatBoost评分回归}
+\text{CatBoost四分类}
+\text{概率融合}.
$$

回归通道预测连续评分：

$$
\widehat H_i=f(\mathbf x_i).
$$

回归预测转换为等级概率 $p_{ik}^{reg}$，分类器输出 $p_{ik}^{cls}$。最终：

$$
p_{ik}^{final}
=0.60p_{ik}^{reg}+0.40p_{ik}^{cls}.
$$

特征准入：

- 保留 `Healthy_Aging_Score`。
- 保留 `Wellness_Category` 作为粗粒度结构先验。
- 排除 `Person_ID`、`Health_Score`。
- 排除与 `Wellness_Category` 完全重复的 `Fitness_Level`。

### 4.3 结果

| 指标 | 数值 |
|---|---:|
| OOF ACC | 0.8927 |
| Macro-F1 | 0.8926 |
| Balanced Accuracy | 0.8925 |
| 平均绝对等级误差 | 0.1073 |
| 评分 RMSE | 1.7113 |
| 评分 MAE | 1.3516 |

补充复跑 ACC 为 0.8926，与主结果相差 0.0001，当前模型冻结。

模型对比：

| 模型 | ACC |
|---|---:|
| CatBoost直接四分类 | 0.8871 |
| CatBoost评分回归后分级 | 0.8908 |
| 0.60回归概率+0.40分类概率 | 0.8927 |

全部 1073 个误判均为相邻等级误判，约 84.7% 集中在距离切点 2 分以内。

500 次 Bootstrap：

| 切点 | 标准差 | 95%区间 |
|---|---:|---:|
| $c_1$ | 0.1703 | [65.638, 66.300] |
| $c_2$ | 0.1596 | [74.200, 74.900] |
| $c_3$ | 0.1651 | [82.500, 83.100] |

### 4.4 正式图表及解释

| 图号 | 图名 | 类型 | 正文解释 | 状态 |
|---|---|---|---|---|
| 图6-1 | 健康评分分布与四级切点 | 单图 | 三个四分位切点形成统一且近似均衡的四级标签 | 已完成 |
| 图6-2 | 问题二 OOF 混淆矩阵 | 单图 | 主对角线占绝对多数，误差仅发生在相邻等级 | 已完成 |
| 图8-3 | 融合权重与模型表现 | 单图 | 回归权重 0.60 取得最高 ACC，最优点附近存在平缓平台 | 已完成 |
| 图8-4 | Bootstrap 切点稳定性 | 3面板 | 三个切点在重抽样下仅小幅波动 | 已完成 |
| 图8-5 | 各健康等级识别指标 | 单图 | Good 类相对最难识别，四类整体仍较均衡 | 已完成 |
| 图8-6 | 边界距离与误判集中性 | 单图 | 误差高度集中于切点邻域，属于离散化边界不确定性 | 已完成 |

图表脚本：

```text
q2_plot_reorganized_cn.m
```

## 5 问题三当前设计

### 5.1 结构先验

问题三预测 `Wellness_Category`，采用两条训练折内估计的结构先验：

1. `Fitness_Level` 多数映射先验：
   $$
   \widehat y_i^F=g_F(Fitness\_Level_i).
   $$

2. `Health_Score` 有序切点先验：
   $$
   \widehat y_i^H
   =\operatorname{digitize}(Health\_Score_i;d_1,d_2,d_3).
   $$

若相邻类别评分严格分离，切点取两侧边界中点；若出现重叠，则在训练折内最小化二分类误差。

### 5.2 统计回退

建立 CatBoost 四分类回退模型，剔除：

- `Person_ID`
- `Wellness_Category`
- `Fitness_Level`
- `Health_Score`

回退模型只依赖其余生活方式、睡眠、运动、饮食、心理、生理和风险字段。

### 5.3 自适应融合

- 双先验一致：
  $$
  p_i=0.49p_i^F+0.49p_i^H+0.02p_i^{ML}.
  $$
- 仅一条先验存在：
  $$
  p_i=0.90p_i^{prior}+0.10p_i^{ML}.
  $$
- 双先验冲突：按训练折可靠度并结合回退概率。
- 双先验均缺失：使用 $p_i^{ML}$。

### 5.4 验证场景

1. 完整双先验；
2. 仅 `Fitness_Level`；
3. 仅 `Health_Score`；
4. 双先验均缺失；
5. `Fitness_Level` 随机缺失 10%；
6. `Health_Score` 随机缺失 10%；
7. 双先验同时缺失 10%；
8. `Fitness_Level` 随机错标 10%；
9. `Health_Score` 加入 $\pm5$ 分扰动。

### 5.5 文件与状态

Python：

```text
问题三求解.py
```

输出：

```text
结果数据表/
└─ 问题三/
   ├─ 问题三求解结果.xlsx
   └─ 问题三OOF预测结果.csv
```

工作簿：

- 核心指标
- 折次结果
- 场景鲁棒性
- 场景折次结果
- Fitness映射
- Health切点
- 类别指标
- 主模型混淆矩阵
- 回退模型混淆矩阵
- OOF样本结果
- 约束违反检查

状态：

- 模型：`designed`
- 结果：`pending`
- 下一门槛：运行问题三代码并核验主模型、回退模型、Poor 类 Recall 和扰动场景。