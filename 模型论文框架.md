# 模型论文框架

> 本文件只保留当前有效口径。模型、参数、约束、数据处理、算法、结果或图表发生变化时，直接替换受影响内容并删除旧版本；历史由 Git 保存。

- 项目：`2026年第五届“创新杯”大学生大数据挑战赛 A题`
- 竞赛与题号：`初赛A：健康生活方式与早起习惯数据分析`
- 框架版本：`v0.2-current`
- 当前阶段：`数据质量检验与特征预处理已完成，进入问题一求解`
- 最近同步：`写入数据审计实测结果，冻结预处理口径与问题一模型`
- 最近同步时间：`2026-07-25`
- 当前状态：`current`

## 当前有效口径

### 题目对象与核心关系

- 研究对象：包含人口统计、睡眠作息、运动行为、饮食营养、心理状态、生理指标、疾病风险及综合健康评价信息的 10000 个个体样本。
- 核心任务：
  1. 预测是否为早起者 `Early_Waker`；
  2. 将连续 `Health_Score` 构造为有序健康等级并分类；
  3. 预测综合健康类别 `Wellness_Category`。
- 评价目标：以三项任务准确率的加权和为竞赛主指标，模型选择优先服从 ACC；辅助报告 F1、Balanced Accuracy、阈值稳定性和有序误判距离。
- 验证原则：所有阈值、编码器、缩放参数、超参数和融合权重均只在训练折内确定，验证折仅用于折外评价。
- 解释边界：模型识别的是当前数据口径下的预测关系，不扩展为因果结论。

### 统一模型主线

采用“数据结构先验驱动的任务适配分类框架”：

$$
\text{数据质量检验与结构识别}
\rightarrow
\text{结构先验数学化}
\rightarrow
\text{任务专属统计学习}
\rightarrow
\text{分层交叉验证与稳定性检验}.
$$

对于存在清晰结构关系的任务，结构先验由训练折估计并直接进入决策函数；对于连续评分有序分级任务，采用训练折内等级构造和有序集成学习。统一预测形式写为

$$
\widehat Y_i=\arg\max_k
\left[
\sum_{m=1}^{M} w_m\log\!\left(p_{ik}^{(m)}+\varepsilon\right)
+\sum_{j=1}^{J}\lambda_j r_{ik}^{(j)}
\right],
$$

其中 $p_{ik}^{(m)}$ 为统计模型概率，$r_{ik}^{(j)}$ 为结构先验得分，$w_m\ge0$、$\sum_m w_m=1$、$\lambda_j\ge0$。当某任务的结构先验已形成零误差分界时，不强制设置无贡献的统计融合权重，而以多算法一致性验证其稳定性。

## 全局数据协议

| 数据源 | 文件 | 规模与字段 | 粒度 | 关联键 | 当前处理口径 |
|---|---|---|---|---|---|
| 赛题说明 | `2026年第五届“创新杯”大学生大数据挑战赛A题(1).pdf` | 任务定义、字段说明、评分规则 | 题目级 | 无 | 作为题意依据 |
| 原始数据 | `A题数据集.csv` | 10000 行、64 列 | 个体级 | `Person_ID` | 原始文件只读 |
| 预处理基础数据 | `预处理数据/A题预处理基础数据.csv` | 10000 行、76 列 | 个体级 | `Person_ID` | 保留原字段并新增时间、运动、饮食、心理和血压衍生变量 |
| 数据审计结果 | `结果数据表/A题数据审计与预处理结果.xlsx` | 12 个工作表 | 字段级/样本级 | 字段名、`Person_ID` | 数据章节与后续特征准入的数值依据 |
| 各问结果 | `结果数据表/问题一至问题三/` | 求解与验证工作簿 | 样本级/模型级 | `Person_ID` | 数值事实最终以标准工作簿为准 |

## 数据质量检验与预处理结论

### 数据完整性

| 检验项目 | 实测结果 | 处理结论 |
|---|---:|---|
| 样本数 | 10000 | 与题目数据一致 |
| 字段数 | 64 | 与预期字段表一致 |
| 数值字段 | 42 | 进入范围与描述统计 |
| 类别/时间字段 | 22 | 按任务采用原生类别或训练折编码 |
| 完全重复行 | 0 | 不删除样本 |
| 重复 `Person_ID` | 0 | 主键唯一 |
| 真实空值单元格 | 0 | 不实施统计插补 |
| 目标字段空值 | 0 | 三个任务均可直接构造标签 |

### 特殊类别语义

数据中的字符串 `None` 为显式类别，不属于缺失值：

| 字段 | `None` 数量 | 比例 | 联合关系 | 当前处理 |
|---|---:|---:|---|---|
| `Exercise_Type` | 824 | 8.24% | 与锻炼频率为 0 的重合率 100% | 保留为“无运动类型”类别 |
| `Workout_Intensity` | 824 | 8.24% | 与锻炼频率为 0 的重合率 100% | 保留为“无运动强度”类别 |
| `Alcohol_Consumption` | 3014 | 30.14% | 无确定缺失机制 | 保留独立类别 |

### 异常值与范围

- 所有设置了物理或业务硬边界的数值字段，硬边界违反数均为 0。
- `Systolic_BP<Diastolic_BP` 的违反数为 0。
- IQR 标记仅表示统计离群，不代表错误；高 BMI、高血压、高血糖、低睡眠质量等极端样本可能是健康风险信号，当前不删除、不统一缩尾。
- 树模型直接使用原尺度；线性或有序模型仅在训练折内标准化。

### 字段派生关系与结构关系

| 关系 | 一致率/相关 | 误差或切点 | 违反数 | 当前用途 |
|---|---:|---|---:|---|
| $BMI=Weight/(Height/100)^2$ | 100% | 最大绝对误差 0.0266 | 0 | 标注为派生冗余变量，保留 |
| 睡眠时长 $=(Wake-Sleep)\bmod24h$ | 100% | 最大误差 0.0167 h | 0 | 验证时间转换正确 |
| `Early_Waker` 与起床时刻 | 100% | 最优阈值 389.5 min | 0 | 问题一结构边界 |
| `Fitness_Level=Wellness_Category` | 100% | 完全一致 | 0 | 问题三第一结构先验 |
| `Health_Score` 对健康类别有序分段 | 100% | 44.95、64.95、79.95 | 0 | 问题三第二结构先验 |
| BMI 与肥胖风险 | Spearman 0.9239 | Low<Medium<High | — | 综合风险输入 |
| 收缩压与高血压风险 | Spearman 0.9298 | Low<Medium<High | — | 综合风险输入 |
| 血糖与糖尿病风险 | Spearman 0.8158 | Low<Medium<High | — | 综合风险输入 |
| 睡眠质量与睡眠障碍风险 | Spearman 0.5291 | High<Medium<Low | — | 综合风险输入 |

### 目标分布

| 目标 | 类别分布 |
|---|---|
| `Early_Waker` | No 5842（58.42%），Yes 4158（41.58%） |
| `Wellness_Category` | Poor 114（1.14%），Average 2137（21.37%），Good 4429（44.29%），Excellent 3320（33.20%） |
| `Health_Score` 四分位候选 | 2515、2508、2491、2486，近似均衡 |

### 预处理基础特征

新增以下候选变量：

$$
Wake\_Min=60h+m,
$$

$$
Wake_{\sin}=\sin\frac{2\pi Wake\_Min}{1440},
\qquad
Wake_{\cos}=\cos\frac{2\pi Wake\_Min}{1440},
$$

并同理构造入睡时间周期变量。行为与健康机制特征包括

$$
Exercise\_Load=Frequency\times Duration,
$$

$$
Sit\_Work\_Ratio=\frac{Sitting}{Working+\varepsilon},
$$

$$
Plant\_Intake=Fruit+Vegetable,
$$

$$
Unhealthy\_Diet\_Load=SugaryDrinks+FastFood,
$$

$$
Mental\_Load\_Raw=Stress+Anxiety+DepressionRisk-Mood,
$$

$$
BP\_Pulse\_Pressure=SBP-DBP.
$$

衍生特征仅作为候选输入，是否进入最终模型由折内消融或交叉验证确定。

### 训练折内处理管道

每个交叉验证外层折严格执行：

1. 分层划分训练折与验证折；
2. 仅使用训练折拟合时间阈值、等级切点、编码器和标准化参数；
3. 在训练折内部选择超参数和融合权重；
4. 对验证折输出 OOF 概率与标签；
5. 模型口径冻结后再用全量数据训练正式模型。

## 全局符号、参数与假设

| 符号 | 类型 | 含义 | 单位/范围 | 当前值或来源 |
|---|---|---|---|---|
| $t_i$ | 状态变量 | 第 $i$ 个样本的起床时刻 | 分钟，$[0,1439]$ | `Wake_Min` |
| $H_i$ | 目标/状态变量 | 健康评分 | 分 | `Health_Score`，实际范围 27.3–100 |
| $Y_i^{(1)}$ | 目标变量 | 是否早起 | Yes/No | 问题一 |
| $Y_i^{(2)}$ | 目标变量 | 健康评分有序等级 | 4 类 | 问题二训练折内构造 |
| $Y_i^{(3)}$ | 目标变量 | 综合健康类别 | 4 类 | 问题三 |
| $\tau$ | 参数 | 时间阈值 | 分钟 | 问题一训练折估计 |
| $s$ | 参数 | 时间边界概率平滑尺度 | 分钟，$s>0$ | 问题一 |
| $\boldsymbol c$ | 参数 | 健康评分分级切点 | 分 | 问题二训练折估计 |
| $w_m$ | 参数 | 模型融合权重 | $[0,1]$ | 训练折内确定 |
| $\lambda_j$ | 参数 | 结构先验权重 | $[0,+\infty)$ | 训练折内确定 |

全局假设：

1. 所有字段在 10000 个样本中的单位和测量口径一致。
2. `Person_ID` 仅承担样本关联作用，不携带可预测的健康顺序信息。
3. 统计离群不等于数据错误，除非违反明确物理或逻辑边界，否则保留。
4. 训练折与验证折来自同一数据生成机制；模型只对当前数据口径负责。
5. 所有数据处理参数均在训练折内拟合。

## 各问依赖关系

| 小问 | 直接目标 | 隐含目标 | 依赖数据 | 输出交付 | 当前状态 |
|---|---|---|---|---|---|
| Q1 | 预测 `Early_Waker` | 识别时间边界、概率置信度与边界稳定性 | 公共预处理、`Wake_Min` | OOF 预测、阈值、ACC、稳定性、多算法一致性 | solving |
| Q2 | 预测健康评分等级 | 构造可解释且可学习的有序等级 | 公共预处理、`Health_Score` 分布 | 切点、OOF 预测、ACC、误判距离、SHAP | designed |
| Q3 | 预测 `Wellness_Category` | 融合两个一致性结构先验与回退模型 | 公共预处理、健康评分结构 | OOF 预测、先验一致率、ACC、回退稳定性 | designed |

# 论文整体框架

## 论文题目候选

1. **基于结构先验与有序集成学习的健康生活方式分类模型**
2. **健康生活方式与综合健康类别的数据结构驱动分类研究**

## 摘要组织

- 总述：数据质量检验、结构先验识别和三项任务适配建模。
- 问题一：时间阈值经验风险、平滑概率和稳定性结果。
- 问题二：健康评分有序分级、累积分类与树模型集成。
- 问题三：体能类别与健康评分双先验、自适应回退预测。
- 综合检验：多折、多随机种子、多算法和扰动稳定性。
- 结论：报告 ACC 和适用边界，不将预测关系解释为因果关系。

## 正文章节

1. 问题背景与重述  
2. 问题分析  
3. 模型假设与符号说明  
4. 数据质量检验与特征预处理  
   4.1 数据集概况与变量分类  
   4.2 数据完整性、唯一性与类型检查  
   4.3 特殊类别与缺失语义识别  
   4.4 描述性统计与合法范围检验  
   4.5 异常值识别与保留原则  
   4.6 字段派生关系与目标结构识别  
   4.7 目标变量分布  
   4.8 时间转换与机制特征构造  
   4.9 类别编码、数值变换与特征准入  
   4.10 训练折内预处理管道  
5. 问题一：最大间隔时间阈值概率模型  
6. 问题二：受约束等级构造与有序集成分类  
7. 问题三：双结构先验自适应融合分类  
8. 模型检验、敏感性与鲁棒性  
9. 模型评价与改进  
10. 结论  
参考文献  
附录  

## 题目要求—论文—程序—结果映射

| 题目要求 | 论文位置 | 核心模型 | Python 程序 | 结果工作簿 | MATLAB 图 |
|---|---|---|---|---|---|
| 公共数据处理 | 第4章 | 完整性、范围、派生关系、折内管道 | `A题数据审计与预处理.py` | `结果数据表/A题数据审计与预处理结果.xlsx` | 后续公共数据图 |
| 早起者分类 | 第5章 | 最大间隔时间阈值概率模型 | `问题一求解.py` | `结果数据表/问题一/问题一求解结果.xlsx` | `结果数据表/问题一/q1_plot.m` |
| 健康评分等级 | 第6章 | 受约束离散化+有序集成 | `问题二求解.py` | `结果数据表/问题二/问题二求解结果.xlsx` | `结果数据表/问题二/q2_plot.m` |
| 综合健康类别 | 第7章 | 双结构先验+回退模型 | `问题三求解.py` | `结果数据表/问题三/问题三求解结果.xlsx` | `结果数据表/问题三/q3_plot.m` |

# 各问模型与结果

## Q1：早起者分类

- 主/次题型：阈值分类 / 概率分类、稳定性验证
- capability：分层交叉验证、阈值稳定性、多算法一致性、扰动鲁棒性
- 当前状态：`solving`
- 结果摘要状态：`pending`

### 当前模型口径

设 $t_i$ 为起床时刻分钟数，$y_i\in\{0,1\}$ 表示是否早起。以经验分类风险最小化估计阈值：

$$
\tau^*
=
\arg\min_{\tau}
\frac1n\sum_{i=1}^{n}
\mathbb I
\left[
\mathbb I(t_i<\tau)\ne y_i
\right].
$$

审计阶段在全样本上识别到候选阈值为

$$
\tau=389.5\ \text{min},
$$

对应 06:29:30，且训练数据中早起者最晚起床时刻为 06:29，非早起者最早起床时刻为 06:30。正式评价必须在每个训练折内重新估计 $\tau$。

定义类别分离间隔：

$$
\Delta
=
\min_{y_i=0}t_i-\max_{y_i=1}t_i.
$$

若 $\Delta>0$，则时间轴上存在严格可分边界。为了输出连续概率，采用平滑边界：

$$
p_i^{time}
=
\frac{1}
{1+\exp((t_i-\tau^*)/s)},
\qquad s>0.
$$

最终标签为

$$
\widehat y_i=\mathbb I(p_i^{time}\ge0.5).
$$

理论深度与验证不依赖堆叠复杂模型，而来自：

1. 经验风险最小阈值估计；
2. 最大类别分离间隔；
3. 概率平滑与 Brier/LogLoss 检验；
4. 5 折、多随机种子阈值稳定性；
5. 决策树桩、逻辑回归、随机森林和梯度提升的一致性验证。

### 路线比较

| 路线 | 核心模型 | 优势 | 局限 | 处理结论 |
|---|---|---|---|---|
| A | 经验风险阈值 + 平滑概率 + 稳定性检验 | 完全对应数据结构，参数可解释，ACC 上限明确 | 对时间测量误差敏感 | 主模型 |
| B | CatBoost/随机森林全特征分类 | 可拟合复杂交互 | 会掩盖单一时间边界，理论解释反而变弱 | 降级为多算法验证 |

### 变量与假设

| 符号 | 类型 | 含义 | 单位 | Python 变量 |
|---|---|---|---|---|
| $t_i$ | 状态变量 | 起床时刻 | 分钟 | `Wake_Min` |
| $y_i$ | 目标变量 | 是否早起 | 0/1 | `Early_Waker` |
| $\tau$ | 参数 | 时间分界 | 分钟 | `threshold` |
| $s$ | 参数 | 概率平滑尺度 | 分钟 | `smooth_scale` |
| $\Delta$ | 评价指标 | 类别分离间隔 | 分钟 | `class_margin` |

假设：

1. 起床时间记录精确到分钟，且同一时间格式统一。
2. 标签含义在所有样本中一致。
3. 阈值只由训练折确定。
4. 平滑尺度只影响概率置信度，不改变阈值决策方向。

### 求解与验证计划

- 主验证：分层 5 折 OOF。
- 稳定性：随机种子 42、73、101、2026、3407，共 25 个外层折。
- 多算法验证：决策树桩、标准化逻辑回归、随机森林、直方图梯度提升。
- 概率指标：Brier Score、LogLoss。
- 鲁棒性：后续对起床时间加入分钟级扰动，识别精度失效区间。
- 结果工作簿：`结果数据表/问题一/问题一求解结果.xlsx`。

### 结果摘要

待 `问题一求解.py` 完成后写入。

## Q2：健康评分有序等级分类

- 主/次题型：有序多分类 / 一维受约束离散化、集成学习
- capability：折内等级切点、单调概率、OOF、多算法、误判距离
- 当前状态：`designed`
- 结果摘要状态：`pending`

### 当前模型口径

问题二先在训练折内确定三个等级切点 $c_1<c_2<c_3$，构造四个近似均衡且类内紧致的健康等级。候选目标为

$$
\min_{\boldsymbol c}
\left[
\sum_{k=1}^{4}\sum_{i\in C_k}(H_i-\mu_k)^2
+\lambda\sum_{k=1}^{4}
\left(\frac{n_k}{n}-\frac14\right)^2
\right].
$$

预测阶段采用累积有序概率模型与 CatBoost/LightGBM 多分类概率集成。审计显示 `Healthy_Aging_Score` 与 `Health_Score` 的 Spearman 相关为 0.9354，是问题二的重要结构变量；其准入和最终权重需在折内验证后冻结。

### 结果摘要

待问题二求解后写入。

## Q3：综合健康类别分类

- 主/次题型：结构先验多分类 / 自适应回退
- capability：先验一致性、冲突回退、OOF、类别召回
- 当前状态：`designed`
- 结果摘要状态：`pending`

### 当前模型口径

审计结果表明：

$$
Fitness\_Level=Wellness\_Category
$$

在 10000 个样本中逐行完全一致；同时

$$
g(Health\_Score)=Wellness\_Category
$$

在切点 44.95、64.95、79.95 下亦达到 100% 一致。问题三采用双先验概率与统计回退模型：

$$
p_{ik}
=
\eta_i p_{ik}^{prior}
+(1-\eta_i)p_{ik}^{ML},
$$

其中两个先验一致时提高 $\eta_i$，字段冲突或缺失时降低 $\eta_i$ 并依赖统计模型。正式模型需在折内估计切点与融合参数。

### 结果摘要

待问题三求解后写入。

# 综合检验与跨问结论

## 多算法一致性

- Q1 将检验不同分类器是否恢复同一时间分界。
- Q2 将比较有序模型、CatBoost、LightGBM 和概率集成。
- Q3 将比较单先验、双先验和回退模型。

## 敏感性与鲁棒性

- Q1：时间测量扰动、概率平滑尺度和阈值折间波动。
- Q2：等级均衡惩罚、切点和融合权重。
- Q3：先验冲突率、字段缺失率和少数类别召回。

## 主要误差来源

1. 任务一时间边界只有 1 分钟间隔，外部测量误差可能破坏完全可分性。
2. 任务二等级标签由连续评分离散化，边界附近样本存在固有不确定性。
3. 任务三 `Poor` 类仅占 1.14%，当结构先验失效时回退模型可能出现少数类漏判。
4. 数据中存在综合评分与派生风险变量，高 ACC 主要反映数据结构一致性，不能直接解释为因果机制。

# 图表证据链

| 图号候选 | MATLAB 图标题 | 图型与作用 | 源工作簿 | 工作表 | 绘图程序 | 支撑结论 |
|---|---|---|---|---|---|---|
| 图4-1 | 主要健康行为与生理指标分布 | 多面板分布图 | 数据审计工作簿 | 描述统计/目标结构 | 待建立公共绘图脚本 | 数据范围与分布 |
| 图4-2 | 目标标签类别分布 | 分组柱形图 | 数据审计工作簿 | 目标结构 | 待建立公共绘图脚本 | 类别均衡与少数类 |
| 图4-3 | 关键字段派生关系与一致性 | 关系图/矩阵 | 数据审计工作簿 | 派生关系 | 待建立公共绘图脚本 | 结构先验来源 |
| 图5-1 | 早起标签与起床时间分界 | ECDF/边界图 | 问题一求解结果 | 边界样本 | `结果数据表/问题一/q1_plot.m` | 时间阈值和间隔 |
| 图5-2 | 问题一交叉验证与阈值稳定性 | 点图/误差棒 | 问题一求解结果 | 折次结果/阈值稳定性 | `q1_plot.m` | 阈值稳定 |
| 图5-3 | 问题一模型预测混淆矩阵 | 热力图 | 问题一求解结果 | 混淆矩阵 | `q1_plot.m` | 分类性能 |

# 待办与缺口

| 优先级 | 待办项 | 完成判据 | 状态 |
|---|---|---|---|
| P0 | 运行问题一主求解 | 生成 OOF、阈值、核心指标和多算法对比 | 进行中 |
| P0 | 更新 Q1 结果摘要 | 写入真实数值和证据位置 | 待求解 |
| P1 | 运行问题一敏感性与鲁棒性 | 输出时间扰动和尺度响应 | 待主模型确认 |
| P1 | 锁定问题二等级构造 | 折内切点稳定且 ACC 优于基准 | 待进行 |
| P2 | 建立 MATLAB 图表 | 从工作簿读取，保留简洁标题 | 待结果冻结 |

# 同步检查

- [x] 文件只保留当前有效模型、参数、约束和数据处理口径；
- [x] 数据审计数值与 `A题数据审计与预处理结果.xlsx` 一致；
- [x] 数据处理已独立成章；
- [x] Q1 当前模型、变量、公式和验证计划已锁定；
- [ ] Q1 结果摘要待求解后同步；
- [ ] MATLAB 图标题、工作表和正文结论待图表阶段同步；
- [x] 当前版本可作为后续代码和论文写作的唯一模型语义入口。
