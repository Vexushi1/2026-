# 模型论文框架

> 本文件只保留当前有效口径。模型、参数、约束、数据处理、算法、结果或图表发生变化时，直接替换受影响内容并删除旧版本；历史由 Git 保存。

- 项目：`2026年第五届“创新杯”大学生大数据挑战赛 A题`
- 竞赛与题号：`初赛A：健康生活方式与早起习惯数据分析`
- 框架版本：`v0.1-current`
- 当前阶段：`模型设计已锁定，准备执行数据质量检验与特征预处理`
- 最近同步：`统一模型主线、独立数据预处理章节、三问模型结构与验证计划`
- 最近同步时间：`2026-07-25`
- 当前状态：`current`

## 当前有效口径

### 题目对象与核心关系

- 研究对象：包含人口统计、睡眠作息、运动行为、饮食营养、心理状态、生理指标、疾病风险及综合健康评价信息的个体样本。
- 核心问题：分别预测早起者标签、健康评分等级和综合健康类别，并以三项分类准确率的加权和作为竞赛评价。
- 显式约束：三个任务均使用参赛者自行划分的训练集与验证集；不得使用验证集标签进行训练或超参数选择；结果文件需包含 `Person_ID`、真实标签和预测标签。
- 隐含约束：问题二的连续健康评分必须先构造具有可解释性的有序等级；问题一和问题三存在强结构关系，模型需利用但不得将其虚构为复杂算法贡献；所有预处理器、等级阈值和集成权重均须在训练折内拟合。
- 统一评价：竞赛主指标为准确率，辅助报告 Macro-F1、Balanced Accuracy、相邻等级误判距离、阈值稳定性和类别召回率。
- 统一精度：概率和评价指标保留 4 位小数；时间统一转换为午夜后的分钟数；连续变量保留原始单位并在表中注明。

### 统一模型主线

采用“数据结构先验驱动的有序集成分类框架”：

$$
\text{数据质量检验与结构识别}
\rightarrow
\text{任务专属概率模型}
\rightarrow
\text{结构先验与统计概率融合}
\rightarrow
\text{分层交叉验证与稳定性检验}.
$$

设第 $m$ 个统计学习器对类别 $k$ 的预测概率为 $p_{ik}^{(m)}$，第 $j$ 个结构先验得分为 $r_{ik}^{(j)}$，统一分类得分写为

$$
S_{ik}
=
\sum_{m=1}^{M}w_m\log\left(p_{ik}^{(m)}+\varepsilon\right)
+
\sum_{j=1}^{J}\lambda_j r_{ik}^{(j)},
$$

并令

$$
\widehat Y_i=\arg\max_k S_{ik},
\qquad
w_m\ge 0,
\qquad
\sum_{m=1}^{M}w_m=1,
\qquad
\lambda_j\ge 0.
$$

权重通过训练折内交叉验证确定，主目标为提高 ACC，并以折间标准差作为次级稳定性约束：

$$
\max_{\boldsymbol w,\boldsymbol\lambda}
\left[
ACC_{\mathrm{CV}}
-\rho\,\operatorname{Std}(ACC_{\mathrm{CV}})
\right].
$$

### 全局数据协议

| 数据源 | 文件/工作表 | 字段与单位 | 粒度 | 关联键 | 当前处理口径 |
|---|---|---|---|---|---|
| 赛题说明 | `2026年第五届“创新杯”大学生大数据挑战赛A题(1).pdf` | 任务定义、字段说明、评分规则 | 题目级 | 无 | 作为题意与字段含义依据 |
| 原始数据 | `A题数据集.csv` | 64列；时间、数值、类别和目标变量 | 个体级，10000行 | `Person_ID` | 原始文件只读；预处理仅在训练折内拟合 |
| 阶段性审计结果 | `结果数据表/数据审计与预处理结果.xlsx` | 完整性、范围、分布、关系、一致性、处理结果 | 字段级/样本级 | `Person_ID`、字段名 | 作为独立数据章节证据；关键结论同步到各问工作簿 `数据审计` 工作表 |
| 各问结果 | `结果数据表/问题一至问题三/` | 核心指标、明细预测、多算法对比、数据审计 | 样本级/模型级 | `Person_ID` | 数值事实最终以各问标准工作簿为准 |

### 全局字段类型与处理原则

| 类型 | 典型字段 | 当前处理原则 |
|---|---|---|
| 标识字段 | `Person_ID` | 仅作结果关联，不进入模型 |
| 时间字段 | `Wake_Up_Time`、`Sleep_Time` | 转为分钟；问题二、三增加正余弦周期编码 |
| 连续数值 | BMI、步数、血压、血糖、评分等 | 合法范围检查；树模型不强制标准化；线性/有序模型在训练折标准化 |
| 有序离散 | 压力、强度、风险等级等 | 保留顺序时使用有序编码，否则按类别变量处理 |
| 无序类别 | 性别、国家、职业、运动类型等 | CatBoost原生类别；其他模型使用训练折编码器 |
| 显式 `None` 类别 | 运动类型、运动强度、饮酒等 | 视为真实类别，不统一替换为缺失值 |
| 综合/派生变量 | BMI、疾病风险、体能等级、健康评分等 | 先检验生成或映射关系，再作为结构先验或模型输入 |
| 目标字段 | `Early_Waker`、`Health_Score`、`Wellness_Category` | 按任务隔离；目标本身不得作为同一任务普通输入 |

### 全局符号、参数与假设

| 符号/参数 | 类型 | 含义 | 单位 | 当前值/范围 | 来源与有效边界 |
|---|---|---|---|---|---|
| $t_i$ | 状态变量 | 第 $i$ 个样本的起床时刻 | 分钟 | $[0,1439]$ | 由时间字符串转换 |
| $H_i$ | 目标/状态变量 | 综合健康评分 | 分 | 以数据实际范围为准 | 任务二原始目标、任务三结构先验 |
| $Y_i^{(1)}$ | 目标变量 | 是否为早起者 | 类别 | Yes/No | 问题一 |
| $Y_i^{(2)}$ | 目标变量 | 健康评分有序等级 | 类别 | 4级 | 由训练折内阈值构造 |
| $Y_i^{(3)}$ | 目标变量 | 综合健康类别 | 类别 | 以原始标签集合为准 | 问题三 |
| $\tau$ | 参数 | 时间或健康评分阈值 | 分钟/分 | 训练折估计 | 不使用验证折拟合 |
| $w_m$ | 参数 | 统计学习器融合权重 | 无量纲 | $[0,1]$ | 训练折内搜索 |
| $\lambda_j$ | 参数 | 结构先验权重 | 无量纲 | $[0,+\infty)$ | 训练折内搜索 |
| $\varepsilon$ | 数值参数 | 防止概率取对数为零 | 无量纲 | $10^{-8}$ | 数值稳定性 |

全局假设：

1. 同一字段在全体样本中的单位和测量口径一致；若审计发现冲突，必须先修正数据协议再建模。
2. `Person_ID` 仅承担样本关联作用，不携带健康或行为顺序信息。
3. 原始极端健康状态可能是真实风险信号，统计离群不等同于错误，不进行无依据的统一删除。
4. 训练折与验证折来自同一数据生成机制；模型结论只适用于该数据口径，不外推为因果关系。
5. 所有等级阈值、编码器、标准化参数、概率校准和集成权重均只由训练折确定。

### 各问依赖关系

| 小问 | 直接目标 | 隐含目标 | 依赖数据/前问 | 输出交付 | 当前状态 |
|---|---|---|---|---|---|
| Q1 | 预测 `Early_Waker` | 识别时间分界并输出稳定概率 | 公共数据预处理；时间结构检验 | OOF预测、阈值、ACC、稳定性、重要性 | designed |
| Q2 | 预测 `Health_Score` 等级 | 构造可解释且可学习的有序等级 | 公共预处理；健康评分分布；Q1无数值依赖 | 等级阈值、OOF预测、ACC、误判距离、SHAP | designed |
| Q3 | 预测 `Wellness_Category` | 识别综合健康标签与上游健康变量的一致性 | 公共预处理；Q2健康评分结构可复用但不依赖其预测结果 | OOF预测、先验一致率、ACC、回退稳定性 | designed |

## 论文整体框架

### 论文题目候选

1. 基于结构先验与有序集成学习的健康生活方式分类模型
2. 健康生活方式与综合健康类别的数据结构驱动分类研究

### 摘要组织

- 总述：说明三项分类任务及“结构识别—有序学习—先验融合”的统一主线。
- 问题一：最优时间阈值、概率平滑与树模型融合；写入最终阈值和 ACC。
- 问题二：受约束等级划分、有序概率模型与 CatBoost/LightGBM 集成；写入阈值、ACC 和相邻误判特征。
- 问题三：一致性结构先验与自适应概率融合；写入一致率、ACC 和回退模型结果。
- 综合检验：多随机种子、折间稳定性、消融和主要误差来源。
- 结论：只陈述数据与模型支持的分类规律，不扩展为因果结论。

### 正文章节

1. 问题背景与重述
2. 问题分析
3. 模型假设与符号说明
4. 数据质量检验与特征预处理
   - 4.1 数据集概况与变量分类
   - 4.2 数据完整性、唯一性与类型检查
   - 4.3 特殊类别与缺失语义识别
   - 4.4 数值变量描述性统计与合法范围检验
   - 4.5 异常值识别、保留与修正规则
   - 4.6 字段派生关系与一致性检验
   - 4.7 目标分布与标签结构识别
   - 4.8 时间转换与机制特征构造
   - 4.9 类别编码、数值变换与特征准入
   - 4.10 训练折内预处理与交叉验证管道
5. 问题一：早起行为的时间边界概率分类模型
6. 问题二：健康评分的受约束有序集成分类模型
7. 问题三：综合健康类别的一致性先验融合模型
8. 模型检验、敏感性与稳定性分析
9. 模型评价与结论
10. 参考文献
11. 附录

### 数据预处理章节的核心证据

| 子节 | 必做分析 | 正文结果 | 阶段性工作簿工作表 |
|---|---|---|---|
| 4.1 | 样本数、字段数、字段类型、单位、目标列 | 数据概况表 | `数据概览`、`字段字典` |
| 4.2 | ID唯一性、重复行、真实空值、非法类型 | 数据质量检查表 | `完整性检查` |
| 4.3 | 字符串 `None` 语义与类别组合关系 | 特殊类别处理表 | `特殊类别` |
| 4.4 | 均值、中位数、标准差、分位数、偏度、范围 | 关键变量描述统计表 | `描述统计` |
| 4.5 | 物理边界、IQR标记、业务合理性 | 异常处理规则及样本数 | `异常与范围` |
| 4.6 | BMI、睡眠时长、风险指标、综合指标映射 | 派生关系一致性表 | `派生关系` |
| 4.7 | 三个目标分布、Q1阈值、Q2候选等级、Q3一致性 | 标签结构表与图 | `目标结构` |
| 4.8 | 时间分钟化、周期编码、运动/饮食/心理负荷 | 特征公式表 | `衍生特征` |
| 4.9 | 模型分支编码与缩放方式 | 特征准入矩阵 | `特征准入` |
| 4.10 | 分层交叉验证、训练折拟合预处理器 | 流程说明与防泄漏检查 | `管道检查` |

### 正文章节与交付映射

| 题目要求 | 论文位置 | 核心公式/模型 | Python 程序 | 结果工作簿/工作表 | MATLAB 图表 | 最终结论 |
|---|---|---|---|---|---|---|
| 公共数据处理 | 第4章 | 范围、派生关系、周期编码、训练折管道 | `A题数据审计与预处理.py` | `数据审计与预处理结果.xlsx` | 待定 | 数据是否完整、哪些结构关系可进入模型 |
| 任务1 | 第5章 | 阈值经验风险、Logistic平滑、概率融合 | `问题一求解.py` | `问题一求解结果.xlsx` | `q1_plot.m` | 早起行为的稳定分类边界及准确率 |
| 任务2 | 第6章 | 受约束离散化、累积有序分类、概率集成 | `问题二求解.py` | `问题二求解结果.xlsx` | `q2_plot.m` | 健康等级阈值、准确率和主要影响特征 |
| 任务3 | 第7章 | 一致性先验、自适应融合、ML回退 | `问题三求解.py` | `问题三求解结果.xlsx` | `q3_plot.m` | 综合健康类别的结构关系与预测结果 |
| 综合评分 | 第8章 | $20ACC_1+40ACC_2+40ACC_3$ | 三问检验脚本 | 三问敏感性与鲁棒性工作簿 | 汇总图待定 | 最终加权成绩和稳定性 |

## 各问模型与结果

### Q1：早起行为预测

- 主/次题型：二分类；阈值识别与概率融合。
- capability：时间阈值一致性、分层交叉验证、折间稳定性、概率校准、消融验证。
- 当前状态：`designed`
- 结果摘要状态：`pending`

#### 当前模型口径

**目标与命题意图**

- 直接目标：预测 `Early_Waker`。
- 隐含目标：识别起床时间与早起标签之间的稳定边界，并避免用复杂模型掩盖确定性结构。
- 评委可能质疑点：满分准确率是否只是标签规则复制；模型是否具有概率输出和异常回退能力。

**路线比较与选择**

| 路线 | 核心模型 | 适配性 | 优势 | 局限/误差 | 难度 | 处理结论 |
|---|---|---|---|---|---|---|
| A | 经验风险最小时间阈值 + Logistic平滑 + CatBoost概率融合 | 高 | 结构清楚、ACC高、可输出概率、可检验阈值稳定性 | 时间结构变化时需回退模型 | 中 | 主模型 |
| B | LightGBM/CatBoost直接分类与软投票 | 中 | 实现成熟、可输出重要性 | 容易将标签结构误写成算法能力 | 低 | 对照验证 |

**变量、参数与假设**

| 符号 | 类型 | 含义 | 单位 | 范围 | Python变量 | 来源 |
|---|---|---|---|---|---|---|
| $t_i$ | 状态 | 起床时刻 | 分钟 | $[0,1439]$ | `wake_min` | `Wake_Up_Time` |
| $y_i$ | 目标 | 是否早起 | 0/1 | $\{0,1\}$ | `y_early` | `Early_Waker` |
| $\tau$ | 参数 | 分类阈值 | 分钟 | 数据驱动 | `threshold_min` | 训练折搜索 |
| $s$ | 参数 | 概率边界平滑尺度 | 分钟 | $s>0$ | `smooth_scale` | 训练折优化 |
| $\omega$ | 参数 | 时间模型权重 | 无量纲 | $[0,1]$ | `blend_weight` | 训练折优化 |

1. 时间字符串可稳定转换为同一天周期内的分钟数；异常格式由审计阶段处理。
2. 阈值关系在训练折和验证折中具有同分布性；通过多折阈值方差检验。
3. CatBoost只承担边界异常和辅助行为信息，不改变可解释的主边界。

**核心公式、目标与约束**

$$
h_\tau(t_i)=
\begin{cases}
1,&t_i<\tau,\\
0,&t_i\ge\tau,
\end{cases}
$$

$$
\tau^*=\arg\min_\tau\frac{1}{n}\sum_{i=1}^{n}\mathbb I\left[h_\tau(t_i)\ne y_i\right],
$$

$$
p_i^{(time)}=\frac{1}{1+\exp\left[(t_i-\tau^*)/s\right]},
$$

$$
p_i=\omega p_i^{(time)}+(1-\omega)p_i^{(cb)},
\qquad
\widehat y_i=\mathbb I(p_i\ge0.5).
$$

- 公式来源：经验风险最小化、Logistic概率边界和概率加权融合。
- 现实含义：用时间边界完成主要判别，用行为模型处理临界样本和结构变化。
- 公式—代码—输出映射：`search_time_threshold()`、`time_probability()`、`fit_fold_model()`；输出阈值、折间误差、OOF概率和分类结果。

**求解与验证方案**

- 主求解：分层5折交叉验证，训练折内搜索 $\tau,s,\omega$。
- 对照算法：决策树、LightGBM、CatBoost。
- 随机种子：`42, 73, 101, 2026, 3407`。
- 必做检验：阈值稳定性、时间格式合法性、规则违反数、融合权重敏感性、完整模型消融。

#### 结果摘要

- 状态：`pending`
- 待写入：最终阈值、ACC、Macro-F1、折间标准差、规则违反数、融合权重及证据位置。

#### 论文与图表映射

| MATLAB 图标题 | DOCX/LaTeX 图注 | 源工作簿 | 工作表与固定列 | MATLAB 脚本 | 支撑结论 | 论文位置 |
|---|---|---|---|---|---|---|
| 早起标签与起床时刻的阈值关系 | 展示两类样本时间分布、最优阈值及边界样本 | `问题一求解结果.xlsx` | `阈值分析` | `结果数据表/问题一/q1_plot.m` | 时间边界的可分性与稳定性 | 5.2 |
| 问题一模型交叉验证性能 | 展示各折ACC与阈值波动 | `问题一敏感性与鲁棒性结果.xlsx` | `算法稳定性` | 同上 | 模型稳定性 | 8.1 |

### Q2：综合健康评分等级预测

- 主/次题型：有序多分类；受约束离散化与概率集成。
- capability：阈值稳定性、单调概率约束、分层交叉验证、相邻误判距离、多算法一致性。
- 当前状态：`designed`
- 结果摘要状态：`pending`

#### 当前模型口径

**目标与命题意图**

- 直接目标：将 `Health_Score` 构造成有序等级并完成多分类预测。
- 隐含目标：等级阈值既要保持类内紧致和可解释性，又要避免严重类别失衡；模型应利用等级顺序减少跨级误判。
- 评委可能质疑点：阈值是否主观；验证集是否参与等级构造；强综合指标是否导致信息捷径。

**路线比较与选择**

| 路线 | 核心模型 | 适配性 | 优势 | 局限/误差 | 难度 | 处理结论 |
|---|---|---|---|---|---|---|
| A | 均衡约束一维最优离散化 + 累积有序分类 + CatBoost/LightGBM概率融合 | 高 | 利用顺序信息，兼顾ACC和跨级误判，理论闭环完整 | 阈值和融合权重需折内嵌套估计 | 中高 | 主模型 |
| B | 普通四分类CatBoost/LightGBM软投票 | 中高 | 实现稳健、精度高 | 忽略等级顺序，跨级误判成本未建模 | 中 | 对照验证 |
| C | TabTransformer/深度表格模型 | 低 | 可建模高阶交互 | 10000样本不足，调参成本高、解释弱 | 高 | 否决为主模型 |

**变量、参数与假设**

| 符号 | 类型 | 含义 | 单位 | 范围 | Python变量 | 来源 |
|---|---|---|---|---|---|---|
| $H_i$ | 目标基础 | 原始健康评分 | 分 | 数据实际范围 | `health_score` | `Health_Score` |
| $Y_i$ | 目标 | 健康有序等级 | 1--4 | 有序类别 | `health_level` | 阈值构造 |
| $\tau_1,\tau_2,\tau_3$ | 参数 | 等级分割点 | 分 | 严格递增 | `cut_points` | 训练折优化 |
| $q_{ir}$ | 中间变量 | $P(Y_i>r\mid X_i)$ | 概率 | $[0,1]$ | `cum_prob` | 累积分类器 |
| $P_{ik}$ | 中间变量 | 类别概率 | 概率 | $[0,1]$ | `class_prob` | 单调投影恢复 |
| $\alpha,\beta$ | 参数 | 有序与多分类通道权重 | 无量纲 | 非负且和不大于1 | `blend_weights` | 训练折搜索 |

1. 健康评分可按连续轴划分为有序等级，等级间存在稳定顺序关系。
2. 阈值仅由训练折估计，验证折不参与标签边界和权重选择。
3. 原始极端健康样本可能是真实状态，不因IQR标记而统一删除。
4. 累积概率应满足单调性；违反时通过最小二乘单调投影修正。

**核心公式、目标与约束**

$$
\min_{\tau_1<\tau_2<\tau_3}
\left[
\sum_{k=1}^{4}\sum_{i\in C_k}(H_i-\mu_k)^2
+\lambda\sum_{k=1}^{4}\left(\frac{n_k}{n}-\frac14\right)^2
\right].
$$

定义累积标签

$$
Z_{ir}=\mathbb I(Y_i>r),\qquad r=1,2,3,
$$

并估计

$$
q_{ir}=P(Y_i>r\mid X_i).
$$

若 $q_{i1}\ge q_{i2}\ge q_{i3}$ 不满足，则求解

$$
(\widetilde q_{i1},\widetilde q_{i2},\widetilde q_{i3})
=
\arg\min_{u_1\ge u_2\ge u_3}
\sum_{r=1}^{3}(u_r-q_{ir})^2.
$$

类别概率恢复为

$$
\begin{aligned}
P(Y_i=1)&=1-\widetilde q_{i1},\\
P(Y_i=2)&=\widetilde q_{i1}-\widetilde q_{i2},\\
P(Y_i=3)&=\widetilde q_{i2}-\widetilde q_{i3},\\
P(Y_i=4)&=\widetilde q_{i3}.
\end{aligned}
$$

最终融合

$$
P_k^{(final)}
=\alpha P_k^{(ordinal)}
+\beta P_k^{(CatBoost)}
+(1-\alpha-\beta)P_k^{(LightGBM)}.
$$

- 公式来源：受约束一维聚类、有序累积链接、保序回归和概率集成。
- 现实含义：阈值保证等级内部相近且比例不过度失衡；有序通道降低跨级误判；树模型捕捉非线性交互。
- 公式—代码—输出映射：`optimize_health_cutpoints()`、`fit_cumulative_classifiers()`、`project_monotone()`、`search_blend_weights()`。

**求解与验证方案**

- 主求解：外层分层5折OOF；训练折内估计阈值、编码器、模型参数和融合权重。
- 对照算法：普通多分类CatBoost、LightGBM、HistGradientBoosting。
- 主要指标：ACC；辅助指标为Macro-F1、Balanced Accuracy和平均等级距离。
- 必做检验：阈值折间波动、概率单调违反数、消融、强综合指标准入、随机种子稳定性。

#### 结果摘要

- 状态：`pending`
- 待写入：最终阈值、类别比例、ACC、Macro-F1、平均等级误差、最优权重、主要特征和证据位置。

#### 论文与图表映射

| MATLAB 图标题 | DOCX/LaTeX 图注 | 源工作簿 | 工作表与固定列 | MATLAB 脚本 | 支撑结论 | 论文位置 |
|---|---|---|---|---|---|---|
| 健康评分的有序等级划分 | 展示评分分布、三个阈值和各等级样本比例 | `问题二求解结果.xlsx` | `等级阈值` | `结果数据表/问题二/q2_plot.m` | 等级划分的紧致性与均衡性 | 6.2 |
| 问题二模型混淆与等级误差 | 展示混淆矩阵及误判距离 | `问题二求解结果.xlsx` | `混淆矩阵`、`等级误差` | 同上 | 误判主要集中于相邻等级 | 6.4 |
| 问题二主要特征贡献 | 展示SHAP绝对贡献与方向 | `问题二求解结果.xlsx` | `特征重要性` | 同上 | 关键健康与生活方式变量 | 6.5 |

### Q3：健康综合类别预测

- 主/次题型：多分类；结构一致性先验与自适应概率融合。
- capability：结构一致性检验、先验冲突率、回退模型有效性、扰动鲁棒性、分层交叉验证。
- 当前状态：`designed`
- 结果摘要状态：`pending`

#### 当前模型口径

**目标与命题意图**

- 直接目标：预测 `Wellness_Category`。
- 隐含目标：识别 `Fitness_Level`、`Health_Score` 与综合健康类别之间的结构关系，并构建对缺失或冲突样本仍可工作的统一分类器。
- 评委可能质疑点：是否简单复制同义字段；结构关系在字段缺失或扰动时是否仍可靠。

**路线比较与选择**

| 路线 | 核心模型 | 适配性 | 优势 | 局限/误差 | 难度 | 处理结论 |
|---|---|---|---|---|---|---|
| A | 双结构先验 + 样本级自适应概率融合 + CatBoost回退 | 高 | 合法利用强信息，同时具备冲突和缺失回退能力 | 需严格报告一致率和扰动稳定性 | 中 | 主模型 |
| B | CatBoost/LightGBM直接多分类与软投票 | 中 | 可独立预测并输出SHAP | 放弃确定性结构后ACC可能下降 | 中 | 回退和对照 |

**变量、参数与假设**

| 符号 | 类型 | 含义 | 单位 | 范围 | Python变量 | 来源 |
|---|---|---|---|---|---|---|
| $F_i$ | 状态 | 体能等级 | 类别 | 数据标签集合 | `fitness_level` | `Fitness_Level` |
| $H_i$ | 状态 | 健康评分 | 分 | 数据实际范围 | `health_score` | `Health_Score` |
| $Y_i$ | 目标 | 综合健康类别 | 类别 | 数据标签集合 | `wellness_category` | `Wellness_Category` |
| $c_i$ | 中间变量 | 两个结构先验是否一致 | 0/1 | $\{0,1\}$ | `prior_consistent` | 审计检验 |
| $\eta_{high},\eta_{low}$ | 参数 | 一致/冲突样本先验权重 | 无量纲 | $0\le\eta_{low}\le\eta_{high}\le1$ | `prior_weights` | 训练折优化 |

1. 结构关系必须先由全样本一致性表验证，再作为先验写入模型。
2. 对字段缺失、非法值和先验冲突样本，使用独立CatBoost概率回退。
3. 先验权重由训练折优化，不直接固定为1；同时执行字段掩蔽鲁棒性测试。

**核心公式、目标与约束**

$$
r_{ik}^{(F)}=\mathbb I(F_i=k),
\qquad
r_{ik}^{(H)}=\mathbb I(g(H_i)=k),
$$

$$
c_i=\mathbb I\left[F_i=g(H_i)\right],
$$

$$
p_{ik}^{(prior)}
=
\frac{r_{ik}^{(F)}+r_{ik}^{(H)}}
{\sum_\ell(r_{i\ell}^{(F)}+r_{i\ell}^{(H)})},
$$

$$
\eta_i=
\begin{cases}
\eta_{high},&c_i=1,\\
\eta_{low},&c_i=0,
\end{cases}
$$

$$
p_{ik}
=
\eta_i p_{ik}^{(prior)}
+(1-\eta_i)p_{ik}^{(ML)},
\qquad
\widehat Y_i=\arg\max_k p_{ik}.
$$

- 公式来源：类别一致性先验、混合专家与样本级门控思想。
- 现实含义：一致样本主要信任综合健康结构，冲突或缺失样本提高统计模型权重。
- 公式—代码—输出映射：`derive_health_prior()`、`check_prior_consistency()`、`fit_fallback_model()`、`adaptive_fusion()`。

**求解与验证方案**

- 主求解：分层5折OOF；训练折内确定健康评分映射和先验权重。
- 对照算法：CatBoost、LightGBM、软投票。
- 必做检验：一致率、先验违反数、10%/20%字段掩蔽、标签噪声扰动、回退模型性能、随机种子稳定性。

#### 结果摘要

- 状态：`pending`
- 待写入：一致率、先验冲突数、ACC、字段掩蔽下ACC、回退模型ACC、融合权重和证据位置。

#### 论文与图表映射

| MATLAB 图标题 | DOCX/LaTeX 图注 | 源工作簿 | 工作表与固定列 | MATLAB 脚本 | 支撑结论 | 论文位置 |
|---|---|---|---|---|---|---|
| 综合健康类别的结构一致性 | 展示体能等级、健康评分映射与目标类别的一致性矩阵 | `问题三求解结果.xlsx` | `结构一致性` | `结果数据表/问题三/q3_plot.m` | 结构先验的有效性 | 7.2 |
| 字段掩蔽下的模型稳定性 | 展示先验字段缺失比例与ACC变化 | `问题三敏感性与鲁棒性结果.xlsx` | `字段掩蔽` | 同上 | 回退机制的鲁棒性 | 8.3 |

## 综合检验与跨问结论

### 多算法与多模型一致性

- 三问均保留至少一个结构清楚的主模型和一个独立统计学习对照模型。
- 所有比较使用同一外层OOF划分；需要内部调参时采用训练折内嵌套验证。
- 不以单次80/20划分的最优结果代替折外预测结果。

### 敏感性与鲁棒性总览

- Q1：时间阈值、平滑尺度和融合权重。
- Q2：等级均衡惩罚参数、三个阈值、单调投影与融合权重。
- Q3：先验权重、字段掩蔽比例和先验冲突比例。
- 全局：五组随机种子和折间标准差。

### 模型适用边界与主要误差来源

- 数据为同源个体横截面记录，不能推断生活方式对健康结果的因果效应。
- 综合指标与基础指标可能存在生成关系，高ACC不等同于模型发现了独立健康机制。
- 任务二的等级定义会改变类别分布和ACC，必须固定最终阈值口径并同步更新全文。
- 任务三结构先验在外部数据中可能失效，需依赖回退模型和字段掩蔽试验界定边界。

### 跨问结论

- 待三问求解后填写，只保留可由工作簿、图表和验证结果定位的结论。

## 图表证据链

| 图号 | MATLAB 图标题 | 图型与作用 | 源工作簿 | 工作表/固定列 | 绘图程序 | 导出文件 | 正文结论 |
|---|---|---|---|---|---|---|---|
| 图4-1 | 关键健康与生活方式变量分布 | 多面板分布图，支持预处理规则 | `数据审计与预处理结果.xlsx` | `描述统计` | 待建立 | 待导出 | 变量分布、偏态与极端状态 |
| 图4-2 | 三个任务的目标类别分布 | 柱状/比例图，说明类别结构 | 同上 | `目标结构` | 待建立 | 待导出 | 类别平衡及少数类风险 |
| 图4-3 | 时间阈值与健康类别结构关系 | 阈值/一致性多面板图 | 同上 | `派生关系`、`目标结构` | 待建立 | 待导出 | Q1、Q3结构先验来源 |
| 图5-1 | 早起标签与起床时刻的阈值关系 | 分布与阈值线 | `问题一求解结果.xlsx` | `阈值分析` | `q1_plot.m` | 待导出 | Q1时间边界 |
| 图6-1 | 健康评分的有序等级划分 | 密度与阈值图 | `问题二求解结果.xlsx` | `等级阈值` | `q2_plot.m` | 待导出 | Q2等级构造 |
| 图7-1 | 综合健康类别的结构一致性 | 一致性矩阵 | `问题三求解结果.xlsx` | `结构一致性` | `q3_plot.m` | 待导出 | Q3结构先验 |

## 待办与缺口

| 优先级 | 待办项 | 影响范围 | 前置条件/数据 | 完成判据 | 状态 |
|---|---|---|---|---|---|
| P0 | 执行数据完整性、类型、范围和特殊类别检查 | 全文第4章、三问输入 | `A题数据集.csv` | 审计工作簿所有表非空，关键异常有处理结论 | pending |
| P0 | 复核BMI、睡眠时长、风险指标与目标结构关系 | Q1、Q2、Q3 | 原始字段 | 输出一致率、误差分布和违反样本数 | pending |
| P0 | 冻结问题二等级阈值优化口径 | Q2 | 健康评分分布 | 给出目标函数、参数范围和训练折内实现 | pending |
| P1 | 建立三个任务最终特征准入矩阵 | 三问模型 | 审计结论 | 每字段有准入/排除/先验/仅审计结论 | pending |
| P1 | 建立数据处理阶段性工作簿 | 第4章 | 审计脚本 | 结果可供MATLAB和正文表格直接读取 | pending |
| P1 | 完成三问求解脚本和两类标准工作簿 | 第5--8章 | 数据协议冻结 | OOF结果、验证和结果摘要齐全 | pending |

## 同步检查

- [x] 文件只保留当前有效模型、参数、约束和结果，不存在新旧版本并存；
- [x] 已独立设置详细的数据质量检验与特征预处理章节；
- [x] 三个问题均给出两条实质路线并锁定当前主模型；
- [x] 每问结果摘要区已建立且未填写未经求解的数字；
- [ ] 数据审计与预处理结果已运行并同步；
- [ ] 变量、假设、公式、Python实现和工作簿字段已核对；
- [ ] 每个已求解小问均有包含核心数值的结果摘要；
- [ ] MATLAB图标题、图注、工作簿和正文结论已形成证据链；
- [x] 本次正式交付已同步完整最新版 `模型论文框架.md`。
