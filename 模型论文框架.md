# 模型论文框架

> 本文件只保留当前有效口径。模型、参数、数据处理、算法、结果或图表变化时，直接替换受影响内容；历史由 Git 保存。

- 项目：`2026年第五届“创新杯”大学生大数据挑战赛 A题`
- 题目：`健康生活方式与早起习惯数据分析`
- 框架版本：`v0.3-current`
- 当前阶段：`数据预处理与问题一主求解已完成，准备问题一敏感性与鲁棒性分析`
- 最近同步：`数据审计实测结果、问题一OOF结果、阈值稳定性和多算法验证`
- 最近同步时间：`2026-07-25`
- 当前状态：`current`

## 当前有效口径

### 题目对象与总体目标

- 研究对象：包含人口统计、睡眠作息、运动、饮食、心理、生理、疾病风险和综合健康评价信息的 10000 个个体样本。
- Q1：预测 `Early_Waker`。
- Q2：将连续 `Health_Score` 构造为有序健康等级并分类。
- Q3：预测 `Wellness_Category`。
- 竞赛主指标：三项 Accuracy 的加权和；模型选择优先服从 ACC。
- 辅助指标：F1、Balanced Accuracy、Brier Score、LogLoss、有序误判距离、阈值稳定性和少数类召回。
- 验证原则：阈值、编码器、缩放参数、超参数和融合权重均只在训练折内确定，验证折仅用于 OOF 评价。
- 解释边界：所有结论均为当前数据口径下的预测关系，不扩展为因果关系。

### 统一模型主线

采用“数据结构先验驱动的任务适配分类框架”：

$$
\text{数据质量检验与结构识别}
\rightarrow
\text{结构先验数学化}
\rightarrow
\text{任务专属统计学习}
\rightarrow
\text{分层交叉验证与稳定性检验}.
$$

统一预测形式为

$$
\widehat Y_i=\arg\max_k
\left[
\sum_{m=1}^{M}w_m\log\left(p_{ik}^{(m)}+\varepsilon\right)
+
\sum_{j=1}^{J}\lambda_jr_{ik}^{(j)}
\right],
$$

其中 $p_{ik}^{(m)}$ 为统计模型概率，$r_{ik}^{(j)}$ 为结构先验得分。若结构先验已经形成零误差分界，则不强制设置无贡献的复杂融合，而以多折、多随机种子和多算法一致性验证其稳定性。

### 全局数据协议

| 数据源 | 文件 | 规模 | 关联键 | 当前口径 |
|---|---|---:|---|---|
| 原始数据 | `A题数据集.csv` | 10000×64 | `Person_ID` | 原始文件只读 |
| 预处理基础数据 | `预处理数据/A题预处理基础数据.csv` | 10000×76 | `Person_ID` | 保留原字段并新增时间和机制特征 |
| 数据审计结果 | `结果数据表/A题数据审计与预处理结果.xlsx` | 12个工作表 | 字段名、`Person_ID` | 第4章与特征准入依据 |
| Q1结果 | `结果数据表/问题一/问题一求解结果.xlsx` | 9个工作表 | `Person_ID` | Q1数值事实源 |
| Q1逐样本结果 | `结果数据表/问题一/问题一OOF预测结果.csv` | 10000行 | `Person_ID` | 真实标签、预测标签、概率和折次 |

### 数据质量检验与预处理结论

#### 完整性与唯一性

| 检验项目 | 结果 | 当前处理 |
|---|---:|---|
| 样本数 | 10000 | 全部保留 |
| 字段数 | 64 | 与预期字段一致 |
| 数值字段数 | 42 | 进行范围和描述统计 |
| 类别/时间字段数 | 22 | 按模型分支编码 |
| 完全重复行 | 0 | 不删除样本 |
| 重复 `Person_ID` | 0 | 主键唯一 |
| 真实空值单元格 | 0 | 不实施统计插补 |
| 目标字段空值 | 0 | 三问均可直接构造标签 |

#### 特殊类别

字符串 `None` 为显式类别，不是缺失值：

| 字段 | 数量 | 比例 | 联合关系 | 处理 |
|---|---:|---:|---|---|
| `Exercise_Type` | 824 | 8.24% | 与锻炼频率为0重合率100% | 保留为无运动类型 |
| `Workout_Intensity` | 824 | 8.24% | 与锻炼频率为0重合率100% | 保留为无运动强度 |
| `Alcohol_Consumption` | 3014 | 30.14% | 无确定缺失机制 | 保留独立类别 |

#### 异常值处理

- 所有设置硬边界的字段违反数均为 0。
- `Systolic_BP<Diastolic_BP` 违反数为 0。
- IQR 离群仅作标记，不机械删除或统一缩尾；高 BMI、高血压、高血糖和低睡眠质量可能是真实风险信号。
- 树模型使用原尺度；线性或有序模型只在训练折内标准化。

#### 派生关系与结构关系

| 关系 | 一致率/相关 | 误差或切点 | 违反数 | 用途 |
|---|---:|---|---:|---|
| $BMI=Weight/(Height/100)^2$ | 100% | 最大误差0.0266 | 0 | 标注派生冗余，保留 |
| 睡眠时长由起床和入睡时刻计算 | 100% | 最大误差0.0167 h | 0 | 验证时间转换 |
| `Early_Waker` 与起床时刻阈值 | 100% | 389.5 min | 0 | Q1结构边界 |
| `Fitness_Level=Wellness_Category` | 100% | 完全一致 | 0 | Q3第一先验 |
| `Health_Score` 对健康类别分段 | 100% | 44.95、64.95、79.95 | 0 | Q3第二先验 |
| BMI 与肥胖风险 | Spearman 0.9239 | Low<Medium<High | — | 综合风险输入 |
| 收缩压与高血压风险 | Spearman 0.9298 | Low<Medium<High | — | 综合风险输入 |
| 血糖与糖尿病风险 | Spearman 0.8158 | Low<Medium<High | — | 综合风险输入 |
| 睡眠质量与睡眠障碍风险 | Spearman 0.5291 | High<Medium<Low | — | 综合风险输入 |

#### 目标分布

- `Early_Waker`：No 5842（58.42%），Yes 4158（41.58%）。
- `Wellness_Category`：Poor 114（1.14%），Average 2137（21.37%），Good 4429（44.29%），Excellent 3320（33.20%）。
- `Health_Score` 四分位候选：2515、2508、2491、2486，近似均衡。

#### 新增候选特征

$$
Wake\_Min=60h+m,
$$

$$
Wake_{\sin}=\sin\frac{2\pi Wake\_Min}{1440},
\qquad
Wake_{\cos}=\cos\frac{2\pi Wake\_Min}{1440},
$$

并同理构造入睡时间周期特征。机制特征包括

$$
Exercise\_Load=Frequency\times Duration,
$$

$$
Sit\_Work\_Ratio=\frac{Sitting}{Working+\varepsilon},
$$

$$
Plant\_Intake=Fruit+Vegetable,
$$

$$
Unhealthy\_Diet\_Load=SugaryDrinks+FastFood,
$$

$$
Mental\_Load\_Raw=Stress+Anxiety+DepressionRisk-Mood,
$$

$$
BP\_Pulse\_Pressure=SBP-DBP.
$$

### 全局符号与假设

| 符号 | 类型 | 含义 | 单位/范围 |
|---|---|---|---|
| $t_i$ | 状态变量 | 起床时刻 | 分钟，$[0,1439]$ |
| $H_i$ | 目标/状态变量 | 健康评分 | 分，实际27.3–100 |
| $Y_i^{(1)}$ | 目标变量 | 是否早起 | Yes/No |
| $Y_i^{(2)}$ | 目标变量 | 健康评分等级 | 4类 |
| $Y_i^{(3)}$ | 目标变量 | 综合健康类别 | 4类 |
| $\tau$ | 参数 | 时间阈值 | 分钟 |
| $s$ | 参数 | 时间概率平滑尺度 | 分钟，$s>0$ |
| $\boldsymbol c$ | 参数 | 健康等级切点 | 分 |
| $w_m$ | 参数 | 模型融合权重 | $[0,1]$ |
| $\lambda_j$ | 参数 | 结构先验权重 | $[0,+\infty)$ |

假设：

1. 所有字段在全体样本中的单位和测量口径一致。
2. `Person_ID` 仅承担样本关联作用。
3. 统计离群不等于错误，除非违反明确物理或逻辑边界，否则保留。
4. 训练折与验证折来自同一数据生成机制。
5. 所有预处理参数和模型参数均只在训练折内拟合。

### 各问依赖关系

| 小问 | 直接目标 | 隐含目标 | 输出 | 状态 |
|---|---|---|---|---|
| Q1 | 预测 `Early_Waker` | 识别时间边界、概率和稳定性 | OOF、阈值、ACC、多算法一致性 | validated |
| Q2 | 预测健康评分等级 | 构造可解释的有序等级 | 切点、OOF、ACC、误判距离、SHAP | designed |
| Q3 | 预测 `Wellness_Category` | 双结构先验与回退 | OOF、先验一致率、ACC、类别召回 | designed |

## 论文整体框架

### 论文题目候选

1. **基于结构先验与有序集成学习的健康生活方式分类模型**
2. **健康生活方式与综合健康类别的数据结构驱动分类研究**

### 正文章节

1. 问题背景与重述
2. 问题分析
3. 模型假设与符号说明
4. 数据质量检验与特征预处理
   - 4.1 数据集概况与变量分类
   - 4.2 完整性、唯一性与类型检查
   - 4.3 特殊类别与缺失语义
   - 4.4 描述统计与范围检查
   - 4.5 异常值识别与保留原则
   - 4.6 派生关系与目标结构识别
   - 4.7 目标分布
   - 4.8 时间转换与机制特征
   - 4.9 编码、变换与特征准入
   - 4.10 训练折内预处理管道
5. 问题一：最大间隔时间阈值概率模型
6. 问题二：受约束等级构造与有序集成分类
7. 问题三：双结构先验自适应融合分类
8. 模型检验、敏感性与鲁棒性
9. 模型评价与改进
10. 结论
参考文献
附录

### 题目要求—程序—结果映射

| 任务 | 论文位置 | 核心模型 | Python | 结果文件 | MATLAB |
|---|---|---|---|---|---|
| 公共数据处理 | 第4章 | 数据质量与结构识别 | `A题数据审计与预处理.py` | `结果数据表/A题数据审计与预处理结果.xlsx` | 公共数据图待建立 |
| Q1 | 第5章 | 最大间隔时间阈值概率模型 | `问题一求解.py` | `结果数据表/问题一/问题一求解结果.xlsx` | `结果数据表/问题一/q1_plot.m` |
| Q2 | 第6章 | 受约束分级+有序集成 | `问题二求解.py` | `结果数据表/问题二/问题二求解结果.xlsx` | `q2_plot.m` |
| Q3 | 第7章 | 双先验+统计回退 | `问题三求解.py` | `结果数据表/问题三/问题三求解结果.xlsx` | `q3_plot.m` |

## 各问模型与结果

### Q1：早起者分类

- 主/次题型：阈值分类 / 概率分类、稳定性验证
- capability：分层OOF、阈值稳定性、多算法一致性、扰动鲁棒性
- 当前状态：`validated`
- 结果摘要状态：`current`

#### 当前模型口径

设 $t_i$ 为起床时刻分钟数，$y_i\in\{0,1\}$ 表示是否早起。阈值通过经验风险最小化确定：

$$
\tau^*=\arg\min_{\tau}
\frac1n\sum_{i=1}^{n}
\mathbb I\left[\mathbb I(t_i<\tau)\ne y_i\right].
$$

定义类别分离间隔

$$
\Delta=\min_{y_i=0}t_i-\max_{y_i=1}t_i.
$$

为输出连续概率，使用

$$
p_i=\frac{1}{1+\exp((t_i-\tau^*)/s)},
\qquad
\widehat y_i=\mathbb I(p_i\ge0.5).
$$

主模型采用折内阈值估计；决策树桩、逻辑回归、随机森林和直方图梯度提升仅用于验证是否恢复同一结构边界。

#### 路线比较

| 路线 | 模型 | 优势 | 局限 | 结论 |
|---|---|---|---|---|
| A | 经验风险阈值+平滑概率+稳定性检验 | 参数可解释，直接对应数据结构 | 对外部时间测量误差敏感 | 主模型 |
| B | 全特征树模型 | 可拟合复杂交互 | 掩盖单一时间边界 | 多算法验证 |

#### 核心结果

训练数据中早起者最晚起床时刻为 06:29，非早起者最早起床时刻为 06:30，因此

$$
\Delta=390-389=1\ \text{min},
$$

最优阈值为

$$
\tau^*=389.5\ \text{min},
$$

等价规则为

$$
Wake\_Up\_Time<06{:}30
\Longrightarrow
\widehat Y=\text{Yes}.
$$

| 指标 | 数值 |
|---|---:|
| OOF ACC | 1.0000 |
| Precision | 1.0000 |
| Recall | 1.0000 |
| F1 | 1.0000 |
| Balanced Accuracy | 1.0000 |
| Brier Score | 0.001870 |
| LogLoss | 0.007891 |
| 误判数 | 0 |

#### 验证结果

- 主分层5折中，每折训练阈值均为 389.5 min，训练和验证误判数均为 0。
- 随机种子 42、73、101、2026、3407 共 25 个外层折全部恢复 389.5 min，验证误判总数为 0。
- 决策树桩、标准化逻辑回归、随机森林和直方图梯度提升的 OOF ACC 均为 1.0000；多数类基准仅为 0.5842。
- 当 $s$ 从 0.25 min 增至 60 min 时，ACC 始终为 1.0000，但 Brier Score 从 0.000152 增至 0.092417，表明平滑尺度只改变概率锐度。
- 10000 个样本均获得一次 OOF 预测，`Person_ID` 唯一，约束检查全部通过。

#### 当前结论

Q1 是存在严格 06:30 时间分界的可分阈值问题。模型满分来源于数据中稳定的一分钟类别间隔，而不是复杂算法。论文应以经验风险、类别间隔、概率平滑和稳定性验证构成理论链，不应把结果包装为树模型的复杂非线性贡献。

#### 证据位置

| 类型 | 文件 | 工作表/函数 | 关键证据 |
|---|---|---|---|
| Python | `问题一求解.py` | `拟合最优阈值`、`时间概率`、`main` | 折内阈值与OOF |
| 工作簿 | `结果数据表/问题一/问题一求解结果.xlsx` | `核心指标` | 阈值389.5、ACC1.0000 |
| 工作簿 | 同上 | `折次结果` | 五折零误判 |
| 工作簿 | 同上 | `阈值稳定性` | 25折阈值一致 |
| 工作簿 | 同上 | `多算法对比` | 四类模型ACC1.0000 |
| 工作簿 | 同上 | `边界样本`、`混淆矩阵` | 一分钟间隔、零误判 |
| 逐样本CSV | `问题一OOF预测结果.csv` | 10000条记录 | 真实/预测标签和概率 |

### Q2：健康评分有序等级分类

- 主/次题型：有序多分类 / 一维受约束离散化、集成学习
- 当前状态：`designed`
- 结果摘要状态：`pending`

先在训练折内确定 $c_1<c_2<c_3$，优化

$$
\min_{\boldsymbol c}
\left[
\sum_{k=1}^{4}\sum_{i\in C_k}(H_i-\mu_k)^2
+
\lambda\sum_{k=1}^{4}\left(\frac{n_k}{n}-\frac14\right)^2
\right].
$$

随后构建累积有序概率模型，并与 CatBoost、LightGBM 多分类概率融合。`Healthy_Aging_Score` 与 `Health_Score` 的 Spearman 相关为 0.9354，其准入与权重需在折内验证后冻结。

### Q3：综合健康类别分类

- 主/次题型：结构先验多分类 / 自适应回退
- 当前状态：`designed`
- 结果摘要状态：`pending`

审计确认

$$
Fitness\_Level=Wellness\_Category
$$

逐行一致，且 `Health_Score` 在 44.95、64.95、79.95 下可完全恢复健康类别。采用双先验概率与统计回退：

$$
p_{ik}=\eta_i p_{ik}^{prior}+(1-\eta_i)p_{ik}^{ML}.
$$

先验一致时提高 $\eta_i$；字段缺失或冲突时降低 $\eta_i$ 并依赖回退模型。

## 综合检验与跨问结论

### 多算法一致性

- Q1：已完成，四类对照算法与阈值模型结论一致。
- Q2：待比较有序模型、CatBoost、LightGBM 和概率集成。
- Q3：待比较单先验、双先验和统计回退。

### 敏感性与鲁棒性

- Q1：下一步检验起床时间测量扰动、阈值偏移和概率尺度。
- Q2：检验均衡惩罚、等级切点和融合权重。
- Q3：检验先验冲突率、字段缺失率和少数类召回。

### 主要误差来源

1. Q1 只有 1 min 类别间隔，外部时间记录误差可能破坏完全可分性。
2. Q2 将连续评分离散化，边界附近存在固有不确定性。
3. Q3 的 Poor 类仅占 1.14%，结构先验失效时回退模型可能漏判少数类。
4. 综合评分与派生风险变量高度同源，高 ACC 不代表因果解释能力。

## 图表证据链

| 图号候选 | MATLAB图标题 | 来源工作簿/工作表 | 作用 | 脚本 |
|---|---|---|---|---|
| 图4-1 | 主要健康行为与生理指标分布 | 数据审计/描述统计 | 数据范围与分布 | 公共绘图脚本待建 |
| 图4-2 | 三项任务目标类别分布 | 数据审计/目标结构 | 类别均衡与少数类 | 公共绘图脚本待建 |
| 图4-3 | 关键字段派生关系与一致性 | 数据审计/派生关系 | 结构先验来源 | 公共绘图脚本待建 |
| 图5-1 | 早起标签与起床时间分界 | Q1/边界样本 | 阈值和一分钟间隔 | `q1_plot.m` |
| 图5-2 | 问题一阈值交叉验证稳定性 | Q1/阈值稳定性 | 25折一致性 | `q1_plot.m` |
| 图5-3 | 问题一分类混淆矩阵 | Q1/混淆矩阵 | 零误判 | `q1_plot.m` |
| 图5-4 | 概率平滑尺度与预测误差 | Q1/平滑尺度检验 | 概率锐度敏感性 | `q1_plot.m` |

## 待办与缺口

| 优先级 | 待办项 | 完成判据 | 状态 |
|---|---|---|---|
| P0 | Q1主求解 | OOF、阈值、核心指标、多算法对比 | 已完成 |
| P0 | Q1结果摘要同步 | 数值和证据位置写入框架 | 已完成 |
| P1 | Q1敏感性与鲁棒性 | 输出时间扰动、阈值偏移和失效区间 | 下一步 |
| P1 | Q1 MATLAB图 | 从工作簿读取并保留标题 | 待敏感性完成 |
| P1 | Q2等级构造 | 折内切点稳定且模型可复现 | 待进行 |
| P2 | Q3双先验模型 | 输出冲突回退和类别召回 | 待进行 |

## 同步检查

- [x] 文件只保留当前有效模型、数据处理和结果。
- [x] 数据审计数值与审计工作簿一致。
- [x] Q1公式、代码、工作簿和结果摘要一致。
- [x] Q1每个已报告数字均可定位到工作簿。
- [ ] Q1敏感性工作簿待生成。
- [ ] MATLAB图标题和图表证据链待正式绘图后复核。
- [x] 本文件可作为后续代码、DOCX和LaTeX写作的唯一模型语义入口。
